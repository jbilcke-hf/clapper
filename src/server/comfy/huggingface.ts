"use server"


export async function run({
  apiKey,
  workflow
}: {
  apiKey: string
  workflow: string
}): Promise<string> {

  // TODO: call Hugging Face
   
  throw new Error(`Hugging Face isn't supported yet`)
}