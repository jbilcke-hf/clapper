// the time resolution - this may be an option in the future
export const DEFAULT_DURATION_IN_MS_PER_STEP = 500

export const DEFAULT_COLUMNS_PER_STEP = 1 // this is dynamic, and corresponds to the zoom level

// how many columns per slice / default segment lenght - this may be an option in the future
// so here, a "slice" (a typical shot)
// lasts for 2 seconds (500ms * 4)
export const DEFAULT_COLUMNS_PER_SLICE = 4

// TODO use this as a vertical zoom level
export const STEP_HEIGHT_COLUNM_RATIO = 1

export const DEFAULT_NB_TRACKS = 24 // 32
